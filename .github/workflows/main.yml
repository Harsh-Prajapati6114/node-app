name: node-app

on: push

jobs:
  test: 
    runs-on: ubuntu-latest
    steps:

      - name: checkout code
        uses: actions/checkout@v4
        
      - name: install dependecies
        run:  npm install
        
      - name: run app
        run: npm start
