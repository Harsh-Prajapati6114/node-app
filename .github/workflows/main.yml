name: node-app

on: push

jobs:
  test: 
    runs-on: ubuntu-latest
    steps:
      - name: install dependecies
        run:  npm install
        
      - name: run tests
        run: npm test

      - name: say hello
        run: echo "helloo good morning"
