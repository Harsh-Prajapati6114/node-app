name: node-app

on: push

jobs:
  build: 
    runs-on: ubuntu-latest
    steps:

      - name: checkout code
        uses: actions/checkout@v4
        
      - name: install dependecies
        run:  npm install

      - name: install Docker
        run: docker --version
